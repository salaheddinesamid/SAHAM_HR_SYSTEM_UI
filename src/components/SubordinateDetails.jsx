import { useState } from "react"

export const SubordinateDetails = ({details})=>{
    const [leaves,setLeaves] = useState(details?.leaves);
    return(
        <div className="row">
            
        </div>
    )
}